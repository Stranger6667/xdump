services:
  - postgresql
language: python
matrix:
  include:
    - python: 3.6
      env:
      - TOX_ENV=lint
    - python: 3.6
      env:
      - TOX_ENV=py36
script:
  - tox -e $TOX_ENV
install:
  - travis_retry pip install tox
after_success:
  - bash <(curl -s https://codecov.io/bash)